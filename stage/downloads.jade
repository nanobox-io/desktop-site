doctype html
html(lang="en")
  
  include ./_head.jade
  
  body
    .main-wrapper
      .downloads
        .left
          h1 Nanobox + Vagrant + Virtual Box
          .descript All in one installer, everything you need in one conenient shot
          .download-btns.apple
            .btn
              .icon
              .details
                .title Mac OSX Intel
                .sub 64 bit
                .down-arrow 845 MB
            .btn
              .icon
              .details
                .title Mac OSX Intel
                .sub 32 bit
                .down-arrow 845 MB
        .right
          h1 Nanobox Only
          .descript Download this if you already have Vagrant & Virtualbox installed
          .download-btns.apple
            .btn
              .icon
              .details
                .title Mac OSX Intel
                .sub 64 bit
                .down-arrow 24 MB
            .btn
              .icon
              .details
                .title Mac OSX Intel
                .sub 32 bit
                .down-arrow 24 MB
      .os-sections
        .apple
          ul
            li v1.4.0	<span>(nightly)</span>
            li v1.39	<span>(beta)</span>
            li v1.3.4	<span>(current stable build)</span>
            li v1.1.9
            li v1.0.0
            li v0.8.233
            li v0.6.0
            li v0.3.2
        .linux
          ul
            li v1.4.0	<span>(nightly)</span>
            li v1.39	<span>(beta)</span>
            li v1.3.4	<span>(current stable build)</span>
            li v1.1.9
            li v1.0.0
            li v0.8.233
            li v0.6.0
            li v0.3.2
        .windows
          ul
            li v1.4.0	<span>(nightly)</span>
            li v1.39	<span>(beta)</span>
            li v1.3.4	<span>(current stable build)</span>
            li v1.1.9
            li v1.0.0
            li v0.8.233
            li v0.6.0
            li v0.3.2
    // build:remove 
    script(src="http://localhost:35729/livereload.js")
    // endbuild
          
